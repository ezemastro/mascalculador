services:
  hormigon:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    # Volumen persistente: SQLite (storage.db + WAL) sobrevive redeploys.
    volumes:
      - hormigon-data:/app/data

volumes:
  hormigon-data: