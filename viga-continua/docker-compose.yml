services:
  viga-continua:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    expose:
      - "80"
    # Volumen persistente: SQLite (storage.db + WAL) sobrevive redeploys.
    volumes:
      - viga-continua-data:/app/data

volumes:
  viga-continua-data: